apiVersion: v1
kind: Service
metadata:
  name: usermgt-webapp-clb-service
  labels:
    app: usermgt-webapp
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: nlb
spec:
  type: LoadBalancer
  selector:
    app: usermgt-webapp
  ports:
  - port:  80
    targetPort: 8080