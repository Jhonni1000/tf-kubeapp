apiVersion: v1
kind: ConfigMap
metadata:
  name: aws-auth
  namespace: kube-system
data:
  mapRoles: |
{{ .Values.awsAuth.mapRoles | indent 4 }}
  mapUsers: |
{{ .Values.awsAuth.mapUsers | indent 4 }}