apiVersion: v1
kind: ConfigMap
metadata:
  name: aws-auth
  namespace: kube-system
  labels:
    app.kubernetes.io/managed-by: Helm
  annotations:
    meta.helm.sh/release-name: platform-bootstrap
    meta.helm.sh/release-namespace: kube-system
data:
  mapRoles: |
{{ .Values.awsAuth.mapRoles | indent 4 }}
  mapUsers: |
{{ .Values.awsAuth.mapUsers | indent 4 }}